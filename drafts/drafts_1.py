def rain(sky):
    for drop in sky:
        drop.down()

def birdFlight(bird, sky, house):
    if house.has_glass and !sky.is_bright:
        bird.rate_of_survival -= 10

def fire_burns():
    smoke.hangs()
    field.at("sunset").turns("bluish-grey")

def sunset():
    birds.wake()
    sky.addColors("dustedGold", pinkishGrey")

while ember.isGlowing:
    if wind.isBlowing:
        ember.dims(10)

while foam.isVisible:
    waves.retreat()
    I.splash.into["a dream"] = 1

def conqueringFear(person, fear):
    if fear == "height":
        person.climb(getHighestFall([randint(2,50) for peak in range(20)]))

def __add__(self, other):
    result = Love(intensity = avg(self.rate + other.rate))
    return result

def heartBeat(time, I, leaves):
    if time.isStill():
        I.die(until=leaves.changeColor("green","black"))

def fades():
    if pink_cushion["dust_level"] == "MODERATE":
        photographer.on("road").stays()

*def bridge_at_night(car, we):
    car.hovers_on = "invisible_line"
    we.go_under = True*

for flower in bee.field_of_view:
    if flower.surface_charge_density < -0.4:
        e_field = generate_electric_field(bee, flower)

*def horizons():
    for meter in horizon.distance:
        cloud.taperOff(meter)*

if glow.atNight == "casts_a_light":
    night = Time("21:00")
    night.create("a_whisper")

*def nightRoad():
    if bus.speeds({"at":"night"}):
        corn.drifts(by = 2) #in inches*

*def changeSeason():
    if summer_leaves.haveFallen():
        sky.setColor("grey")*

def colorSeen(human, cloud):
    if cloud.coord + human.coord < 5:
        return("grey")

house = Structure("cube", {'x':2, 'y': 5, 'z': 2} )
hill = terrain.getHighestPeak()
house.setOn(hill)

