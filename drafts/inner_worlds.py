def simple_diffusion(self, particle):
    if particle in low_concentration_area:
        self.particle.mo

def can_diffuse_passively(self, molecule):
    if not (molecule.name in ["hydrogen","co2"] or molecule.type in ["lipid_soluble, steroid_hormones"]):
        return False
