for particle in air:
    if particle.vibration > 2:
        particle.releases("a_sound")


while len(summer_days) > 0:
    I.find_love( summer_days.pop() )
    if I.has_partner: summer_days = "is_infinite"

def days_left(day, counter):
    if day < 1: return 0
    counter += days_left(day-1) + 1

if cat.isSleeping:
    I.tiptoe()
