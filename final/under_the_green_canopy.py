##current poem count: 20

def boreal_forest():
    if siberian_tiger.is_searching_for_food():
        boreal_forest.is_quiet = True

while flower.is_growing():
    petal.starts_forming()
    petal.awaits("bloom")

def macarenia_blooms(self, sunlight):
    if sunlight.reaches(self):
        river_bed.bursts_with_color("red")

def wild_bore_eats(meal):
    if meal == "pine cones":
        siberian_tiger.can_attack = True
    
with open('jaguar_coordinates.txt','r') as path:
    y_coordinates = [int(coordinate[1]) for coordinate in path.readlines()]
    jaguar.is_swimming = False if mean(y_coordinates) > 0.5 else True

def cast_sunset(scene):
    if scene == "jungle":
        wisp_of_fog.over(ondulating_forest).glows()

def find_freshwater_mammals(season):
    if season == "winter":
        return Manatee()

if not mayfly.is_larvae():
    mayfly.emerges(over="Hungary's Tisza River")
    scene.paint("flickering #FFDB58 wings")

while frog.waits:
    frog.expects(mate)
    if not mate.exists: break

for meter in river.distance:
    if river.hits_rock:
        break()

def reach_spawning_ground(salmon):
    river.is_rushing_down = True
    salmon.swim("upriver")

class Porous_Rock:
    def __init__(self, fresh_water):
        self.under = fresh_water
        
def reproduce(siamese_fighting_fish):
    male_fish.make_bubble_raft()
    both_fish.embrace()

for bubble in pond_surface.bubble_raft:
    if fish.has_egg():
        bubble.append(fish.eggs.pop())

def hunt_for_fish(bald_eagle):
    bald_eagle.flies_up()
    await other_eagle.crashes_into(bald_eagle)

### UNORDERED 

def hunt(echo):
    while echo:
        bambi.loses(bambi.mom)

def pollinates(bee, pollen):
    while bee.visits_plant():
        bee.catches(pollen)

while fire.rages_on():
    smoke.accumulates()
    forest.loses(Life("on_ground"))

def mitigate_war(bonobo_male_pair):
    a, b = bonobo_male_pair
    mate(a,b)

if moon.is_full() and (
    forest.feels_empty):
    wolf.howls()